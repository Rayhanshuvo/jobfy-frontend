# .gitpod.yml
image:
  file: .gitpod.Dockerfile

tasks:
  - init: |
      echo "⚡ Installing dependencies..." 
      npm install
      npm run build
  - command: npm start

ports:
  - port: 3000
    onOpen: open-browser
